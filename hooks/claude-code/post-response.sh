#!/usr/bin/env bash
# Claude Code `PostResponse` hook — optional realtime ingest during long sessions.
# Disabled by default; enable by symlinking into ~/.claude/hooks/ when P1 ships.

set -euo pipefail

exit 0
